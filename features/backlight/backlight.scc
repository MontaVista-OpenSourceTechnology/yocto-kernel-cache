# SPDX-License-Identifier: MIT
define KFEATURE_DESCRIPTION "Enable display backlight support"
define KFEATURE_COMPATIBILITY board

kconf hardware backlight.cfg

if [ "$KARCH" = "arm64" ]; then
    kconf hardware backlight-arm64.cfg
fi
